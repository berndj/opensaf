# This file contains configuration for the SAF NTF service
# See the file services/ntfsv/README for more configuration options.

# Uncomment the next line if you want to run the log server through valgrind
#export TOOL="valgrind --leak-check=full --log-file=/tmp/ntf.valgrind"

# Uncomment the next line to enable traces from the NTF server
#export NTFSV_TRACE_CATEGORIES=ALL

export NTFSV_TRACE_PATHNAME="$NCS_STDOUTS_PATH/opensaf_ntfd.log"
